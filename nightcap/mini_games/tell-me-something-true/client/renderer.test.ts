// This package-local test documents the expected renderer behavior. The
// runnable coverage lives in nightcap-web/tests/mini-game-tmst-renderer.test.ts
// so it can execute through the Nightcap web test bundle without changing the
// renderer TypeScript project shape.

export {};
