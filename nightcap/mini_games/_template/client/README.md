# Client Presentation

Place browser presentation prototypes here. The stable renderer interface and
Cloudflare build integration belong to AW-253. Until then, code in this folder
must not be imported by the Python engine or treated as a canonical runtime.
