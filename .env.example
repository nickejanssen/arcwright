POSTGRES_HOST=127.0.0.1
POSTGRES_PORT=5433
POSTGRES_DB=arcwright
POSTGRES_USER=arcwright
POSTGRES_PASSWORD=arcwright
DATABASE_URL=
FIREBASE_PROJECT_ID=arcwright-prod
FIREBASE_WEB_API_KEY=
ARCWRIGHT_API_KEY=
LITELLM_DEFAULT_ROUTING_TABLE=config/routing_table.json
CONTENT_LOGGING_ENABLED=false
# LLM credentials. Either fill these, or fill the provider-neutral slots below
# that cloud deploy binds — the routing layer treats them as equivalent and
# `make rehearsal` accepts whichever pair is set. Do not set both.
ANTHROPIC_API_KEY=
GROQ_API_KEY=
# PRIMARY_LLM_API_KEY=
# SECONDARY_LLM_API_KEY=
