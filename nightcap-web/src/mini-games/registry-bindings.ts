// Placeholder. The real bindings are injected at bundle time by
// scripts/discover-mini-games.mjs via an esbuild virtual module. Source
// stays minimal so the worker tsc pass does not chase imports across the
// repo into mini-game packages.

import "./registry.js";
