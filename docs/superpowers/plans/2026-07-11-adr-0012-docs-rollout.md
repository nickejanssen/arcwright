# ADR-0012 Documentation and Tracker Rollout Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers-extended-cc:subagent-driven-development (recommended) or superpowers-extended-cc:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Roll ADR-0012 (authorial intent block, narrative obligations model, continuity/coherence eval suite) through every canonical surface: roadmap task files, new epic M5-H, index.json, spec files, architecture sections, PRD sections, and GitHub issues.

**Architecture:** Docs-only change set. No engine code. Each task produces committable file changes verified by grep/JSON-parse checks; the final task creates GitHub issues and backfills live issue numbers into the roadmap manifest per the tracker cross-reference policy (canonical IDs live in markdown, live GitHub numbers live in the manifest only).

**Tech Stack:** Markdown, JSON (`docs/roadmap/index.json`), `gh` CLI, Python one-liners for JSON validation.

**User decisions (already made):**
- Adopt improvements 1 (authorial intent block), 2 (narrative obligations model), 4 (continuity/coherence evals); items 3 and 5 are open-question log entries only. Recorded in ADR-0012 (Accepted) and D-068.
- Milestone placement: new epic M5-H holds all three tasks; AW-272 (evals) is pre-M6 and joins the M5 exit gate; AW-270 and AW-271 are flagged (post-M6) like AW-245.
- No em dashes in any created or edited file (AGENTS.md, bound on all agents).
- Platform docs stay game-agnostic: Nightcap appears only as an example, never as an engine-level assumption.

**Repo rules that bind every task:** branch off `main` first; commit per task; never stage `.claude/`/tool-local files; `docs/roadmap/operations/cloud-deploy-runbook.md` has pre-existing uncommitted changes that must NOT be staged.

---

## File Structure

| File | Action | Task |
| --- | --- | --- |
| `docs/roadmap/tasks/AW-270-authorial-intent-block-and-fidelity-telemetry.md` | Create | 1 |
| `docs/roadmap/tasks/AW-271-narrative-obligations-model.md` | Create | 1 |
| `docs/roadmap/tasks/AW-272-continuity-coherence-eval-suite.md` | Create | 1 |
| `docs/roadmap/epics/M5-H-narrative-fidelity-layer.md` | Create | 2 |
| `docs/roadmap/milestones/M5-hardening-proof-prerequisites.md` | Modify (epic list + exit gate) | 2 |
| `docs/roadmap/index.json` | Modify (M5 epics array, new epic entry, 3 task entries) | 3 |
| `docs/specs/0064-aw-270-authorial-intent-block.md` | Create | 4 |
| `docs/specs/0065-aw-271-narrative-obligations-model.md` | Create | 4 |
| `docs/specs/0066-aw-272-continuity-coherence-evals.md` | Create | 4 |
| `docs/architecture/03-arc-execution.md` | Modify (append section 3.8) | 5 |
| `docs/architecture/11-telemetry.md` | Modify (append section 11.8) | 5 |
| `docs/architecture/supplemental-schemas.md` | Modify (append `obligations` section) | 5 |
| `docs/prd/02-requirements.md` | Modify (append Narrative fidelity paragraph) | 6 |
| `docs/prd/03-scope.md` | Modify (append Future Directions bullet) | 6 |
| GitHub: 1 epic issue + 3 task issues; `index.json` backfill | Create/Modify | 7 |

Precedent note: the "Do not edit this file directly; edit in Notion and re-sync" headers on PRD/architecture files are stale convention; AW-256 (commit `ca5ea53`) and the 03-scope Future Directions section were both edited in-repo. Follow the in-repo precedent.

---

### Task 1: Branch + three roadmap task files

**Goal:** Create the working branch and the three canonical task files AW-270, AW-271, AW-272 in the established task-file format (model: `docs/roadmap/tasks/AW-267-nightcap-art-direction-brief.md`).

**Files:**
- Create: `docs/roadmap/tasks/AW-270-authorial-intent-block-and-fidelity-telemetry.md`
- Create: `docs/roadmap/tasks/AW-271-narrative-obligations-model.md`
- Create: `docs/roadmap/tasks/AW-272-continuity-coherence-eval-suite.md`

**Acceptance Criteria:**
- [ ] Branch `docs/adr-0012-rollout` exists and is checked out
- [ ] All three task files exist with sections: Milestone / Epic, Size, Status, Plain-English Summary, Why This Matters, Player Impact, Business Value, Technical Scope, Acceptance Criteria, Tests/Verification, Dependencies, Must Not Do, Architecture References, Playtest Relevance
- [ ] AW-270 and AW-271 are marked post-M6; AW-272 is not
- [ ] No em dash character appears in any of the three files

**Verify:** `grep -c "^## " docs/roadmap/tasks/AW-270*.md docs/roadmap/tasks/AW-271*.md docs/roadmap/tasks/AW-272*.md` → 12 sections per file; `grep -l $'—' docs/roadmap/tasks/AW-27[012]*.md` → no output

**Steps:**

- [ ] **Step 1: Confirm branch**

The branch `docs/adr-0012-rollout` was created at planning handoff and already contains the ADR-0012 scribe records (ADR file, decisions README index, both product log CSVs) and this plan's files as its first commit. Confirm you are on it:

```bash
git branch --show-current
```

Expected: `docs/adr-0012-rollout`. Only if it does not exist: `git checkout -b docs/adr-0012-rollout main` and verify `docs/decisions/0012-authorial-intent-obligations-continuity-evals.md` is present and committed before continuing.

- [ ] **Step 2: Write AW-270 task file**

Write `docs/roadmap/tasks/AW-270-authorial-intent-block-and-fidelity-telemetry.md`:

```markdown
# AW-270: Authorial Intent Block and Intent Fidelity Telemetry (post-M6)

**Milestone / Epic:** M5 / M5-H
**Size:** M
**Status:** Planned (post-M6)

## Plain-English Summary

Add an optional `authorial_intent` block to the ArcDefinition schema carrying
theme, tone, and per-beat emotional targets (a target tension value per beat).
The engine injects the block into generation context assembly and logs
realized-versus-intended tension per beat so arc authors can see whether
sessions delivered the emotional shape they designed.

## Why This Matters

ADR-0012 identified this as the missing structured representation of soft
authorial logic. The pacing engine computes a live dramatic tension score
today but has no authored target curve to compare it against. This deepens
human arc primacy: the author declares intended emotional shape, the runtime
measures fidelity to it.

## Player Impact

Indirect. Sessions track closer to the emotional arc the author designed,
and future arc tuning is driven by fidelity data instead of guesswork.

## Business Value

Realized-versus-intended tension curves are an additional Tier 2 training
signal collected at near-zero marginal cost, strengthening the proprietary
session-data moat described in `docs/architecture/11-telemetry.md`.

## Technical Scope

- Extend ArcDefinition schema validation (AW-203 lineage) with an optional
  `authorial_intent` block: `theme` (string), `tone` (string),
  `emotional_targets` (list of `{beat_id, target_tension, note}`).
- Schema validation rejects `emotional_targets` entries whose `beat_id` is
  not in the arc's beat graph and `target_tension` outside [0.0, 1.0].
- Context assembly includes the block in generation prompts as a cacheable
  context layer per the prompt-caching requirement.
- When `emotional_targets` is present, `tension_update` telemetry events
  gain a `target_score` payload field, and beat exit emits
  `intent_fidelity_summary` with `{beat_id, target_score, mean_score,
  mean_abs_deviation}`.
- Existing arcs without the block remain valid; no behavior change when the
  block is absent.

## Acceptance Criteria

- [ ] ArcDefinition schema accepts and validates the optional
  `authorial_intent` block per spec 0064.
- [ ] An arc without the block validates and runs unchanged.
- [ ] With the block present, `tension_update` events carry `target_score`
  and beat exits emit `intent_fidelity_summary`.
- [ ] Unit tests cover schema validation, context assembly inclusion, and
  both telemetry payloads.

## Tests/Verification

- `pytest engine/tests/` passes with new schema and telemetry tests.
- Headless harness run with an intent-bearing arc shows both new event
  types in the events log.

## Dependencies

- `docs/specs/0064-aw-270-authorial-intent-block.md`
- ADR-0012, `docs/architecture/03-arc-execution.md` Section 3.8

## Must Not Do

- Do not let intent fields override deterministic state transitions; intent
  is generation context and telemetry comparison only.
- Do not make the block mandatory for any arc.
- Do not hardcode any game-specific beat IDs or theme vocabulary in the
  engine.
- Do not start implementation before M6 exit unless the founder re-sequences.

## Architecture References

- `docs/decisions/0012-authorial-intent-obligations-continuity-evals.md`
- `docs/architecture/03-arc-execution.md` Sections 3.3 and 3.8
- `docs/architecture/11-telemetry.md` Section 11.8

## Playtest Relevance

Post-M6. Provides the fidelity instrumentation used to tune arcs after the
first qualifying sessions.
```

- [ ] **Step 3: Write AW-271 task file**

Write `docs/roadmap/tasks/AW-271-narrative-obligations-model.md`:

```markdown
# AW-271: Narrative Obligations Model and Reveal-Readiness Condition (post-M6)

**Milestone / Epic:** M5 / M5-H
**Size:** M
**Status:** Planned (post-M6)

## Plain-English Summary

Track narrative obligations (authored setups, promises requiring payoff, and
any pacing-engine misdirection injection) as durable session state, and
expose a generic `all_mandatory_obligations_resolved` boolean in the session
context so arcs can use it as a beat exit condition.

## Why This Matters

ADR-0012 identified an untracked coherence hole: when the pacing engine
injects a generative red herring, no platform state records that the
misdirection exists and should be acknowledged or resolved before the arc's
resolution beat. Obligations close that hole with deterministic state,
consistent with the principle that AI never manages canonical session state.

## Player Impact

Sessions stop leaving dangling threads: an injected misdirection is
guaranteed to be addressed before the resolution beat when the arc marks it
mandatory.

## Business Value

Obligation lifecycle telemetry (created, resolved, expired) becomes a
coherence quality signal across sessions and a Tier 2 training input.

## Technical Scope

- New `obligations` table per `docs/architecture/supplemental-schemas.md`;
  requires an Alembic migration (schema-change hard rule: migration plus
  review per `AGENTS.md`).
- Obligation records carry `source_type` (`authored`, `pacing_misdirection`,
  `generative`), `mandatory` flag, `status` (`open`, `resolved`, `expired`),
  creating beat, and resolving beat.
- Pacing-engine misdirection injection creates an obligation record
  automatically; whether it is mandatory is arc-configurable.
- Session context exposes `all_mandatory_obligations_resolved`, evaluated
  generically like all exit conditions per the contract in
  `docs/architecture/03-arc-execution.md` Section 3.2. No beat IDs or
  obligation names in engine code.
- Telemetry: `obligation_created` and `obligation_resolved` events per
  `docs/architecture/11-telemetry.md` Section 11.8.

## Acceptance Criteria

- [ ] Migration applies cleanly to empty and populated schemas.
- [ ] Pacing misdirection injection writes an obligation record.
- [ ] `all_mandatory_obligations_resolved` appears in session context and
  gates a test arc's exit condition correctly.
- [ ] Both telemetry events are emitted and logged to the events table.
- [ ] Unit tests cover obligation lifecycle, the session-context boolean,
  and telemetry emission.

## Tests/Verification

- `pytest engine/tests/` passes with obligation lifecycle tests.
- `alembic upgrade head` applies the migration cleanly.
- Headless harness run shows an injected misdirection producing an
  obligation record and the reveal gate holding until resolution.

## Dependencies

- `docs/specs/0065-aw-271-narrative-obligations-model.md`
- ADR-0012, `docs/architecture/03-arc-execution.md` Section 3.8

## Must Not Do

- Do not let AI generation create or resolve obligations directly; only
  deterministic engine paths mutate obligation state.
- Do not hardcode game-specific obligation names or beat IDs in engine code.
- Do not skip the migration review hard rule.
- Do not start implementation before M6 exit unless the founder re-sequences.

## Architecture References

- `docs/decisions/0012-authorial-intent-obligations-continuity-evals.md`
- `docs/architecture/03-arc-execution.md` Sections 3.2 and 3.8
- `docs/architecture/supplemental-schemas.md` (`obligations`)
- `docs/architecture/11-telemetry.md` Section 11.8

## Playtest Relevance

Post-M6. Hardens coherence for the sessions that follow the first
qualifying playtests.
```

- [ ] **Step 4: Write AW-272 task file**

Write `docs/roadmap/tasks/AW-272-continuity-coherence-eval-suite.md`:

```markdown
# AW-272: Continuity and Coherence Eval Suite

**Milestone / Epic:** M5 / M5-H
**Size:** M
**Status:** Planned

## Plain-English Summary

Build a continuity eval suite on the existing eval harness and headless
session runner that replays synthetic sessions and reports, at minimum,
knowledge-leak rate (an AI character referencing a fact outside its
knowledge state) and character-contradiction count.

## Why This Matters

The mandatory pre-generation knowledge query constrains prompts, but nothing
verifies the model actually respected the constraint. ADR-0012 adopted
offline evals as the first consumer of the Tier 2 "behavior consistency
score" signal. This also contributes to the open product question requiring
narrative quality metrics before the first M3 simulation-harness batch.

## Player Impact

Knowledge leaks and character contradictions are caught on synthetic
sessions before real groups ever see them.

## Business Value

A repeatable narrative-quality gate raises confidence going into M6
qualifying sessions and produces the labeled continuity data that the
Tier 2 transition needs.

## Technical Scope

- New eval runner at `evals/runners/test_continuity_evals.py` following the
  AW-224 batch-harness and spec 0004 eval-harness patterns.
- Deterministic checks first: fact-payload matching between generated
  character output and the character's knowledge state at generation time.
  No model-graded checks in v1 (cost policy; see spec 0066).
- Metrics emitted as a JSON report: `knowledge_leak_rate`,
  `contradiction_count`, per-session and batch aggregate.
- Configurable pass thresholds; suite is runnable locally and in CI batch
  mode.

## Acceptance Criteria

- [ ] `pytest evals/runners/test_continuity_evals.py -q` runs against a
  recorded or freshly generated synthetic session batch and produces the
  JSON report.
- [ ] A seeded session with a deliberate injected leak is detected
  (true-positive test); a clean session reports zero leaks
  (false-positive guard).
- [ ] Thresholds are configurable without code changes.
- [ ] Report format documented in spec 0066.

## Tests/Verification

- `pytest evals/runners/test_continuity_evals.py -q` passes.
- Batch run over at least 10 synthetic sessions completes and the report
  aggregates correctly.

## Dependencies

- `docs/specs/0066-aw-272-continuity-coherence-evals.md`
- Headless session runner (AW-110/AW-112) and batch harness (AW-224)

## Must Not Do

- Do not use frontier-model graded evals in v1; deterministic checks only
  per the AI cost policy.
- Do not couple checks to any game-specific fact vocabulary; operate on
  knowledge-graph structures only.
- Do not block M6 qualifying sessions on new eval infrastructure beyond the
  M5 exit-gate line added for this task.

## Architecture References

- `docs/decisions/0012-authorial-intent-obligations-continuity-evals.md`
- `docs/architecture/04-knowledge-graph.md` Section 4.3
- `docs/architecture/11-telemetry.md` Sections 11.6 and 11.8

## Playtest Relevance

Pre-M6. Runs against synthetic session batches before qualifying sessions
so continuity regressions surface before real groups play.
```

- [ ] **Step 5: Verify and commit**

Run: `grep -l $'—' docs/roadmap/tasks/AW-270*.md docs/roadmap/tasks/AW-271*.md docs/roadmap/tasks/AW-272*.md || echo CLEAN`
Expected: `CLEAN`

```bash
git add docs/roadmap/tasks/AW-270-authorial-intent-block-and-fidelity-telemetry.md docs/roadmap/tasks/AW-271-narrative-obligations-model.md docs/roadmap/tasks/AW-272-continuity-coherence-eval-suite.md
git commit -m "docs(roadmap): add AW-270/271/272 task files for ADR-0012 narrative fidelity layer

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 2: Epic M5-H file + M5 milestone update

**Goal:** Create the M5-H epic file and register it in the M5 milestone file, adding the AW-272 line to the M5 exit gate.

**Files:**
- Create: `docs/roadmap/epics/M5-H-narrative-fidelity-layer.md`
- Modify: `docs/roadmap/milestones/M5-hardening-proof-prerequisites.md` (Epics list, Exit Gate list)

**Acceptance Criteria:**
- [ ] Epic file follows the M5-C section format (Plain-English Summary through Playtest Relevance)
- [ ] Milestone file lists M5-H after M5-G and the exit gate includes the continuity eval line
- [ ] No em dashes in created or edited content

**Verify:** `grep -c "M5-H" docs/roadmap/milestones/M5-hardening-proof-prerequisites.md` → 1; `grep "continuity" docs/roadmap/milestones/M5-hardening-proof-prerequisites.md` → exit gate line present

**Steps:**

- [ ] **Step 1: Write epic file**

Write `docs/roadmap/epics/M5-H-narrative-fidelity-layer.md`:

```markdown
# M5-H: Narrative Fidelity Layer

**Milestone:** M5  
**Status:** Planned

## Plain-English Summary

Adopt the three ADR-0012 narrative fidelity improvements: a continuity and
coherence eval suite (pre-M6), an optional authorial intent block in the
ArcDefinition schema with intent fidelity telemetry (post-M6), and a
narrative obligations model with a generic reveal-readiness condition
(post-M6).

## Why This Matters

ADR-0012 compared the platform against the dissertation-derived story-logic
framework and adopted three gaps as scope: no structured soft authorial
logic, no durable tracking of narrative obligations (including
pacing-injected misdirection), and no offline verification that generation
respected knowledge-state constraints. This epic closes them without
touching the M6 qualifying-session critical path.

## Player Impact

Sessions deliver the emotional shape the author designed, dangling threads
get resolved before the reveal, and continuity regressions are caught on
synthetic sessions before real groups play.

## Business Value

Intent fidelity curves and obligation lifecycle data are additional Tier 2
training signals; the eval suite is a repeatable narrative-quality gate that
raises confidence going into qualifying sessions.

## Tasks

- [AW-272: Continuity and Coherence Eval Suite](../tasks/AW-272-continuity-coherence-eval-suite.md)
- [AW-270: Authorial Intent Block and Intent Fidelity Telemetry](../tasks/AW-270-authorial-intent-block-and-fidelity-telemetry.md) (post-M6)
- [AW-271: Narrative Obligations Model and Reveal-Readiness Condition](../tasks/AW-271-narrative-obligations-model.md) (post-M6)

## Acceptance Criteria

- All child tasks satisfy their acceptance criteria.
- AW-272 completes before M6 qualifying sessions; AW-270 and AW-271 remain
  sequenced post-M6 unless the founder re-sequences.
- The milestone exit gate remains consistent with `docs/roadmap/00-overview.md`.

## Tests/Verification

- Verify every child task is complete and has evidence linked from its task
  issue.
- Verify the parent milestone exit gate still matches
  `docs/roadmap/00-overview.md`.

## Dependencies

- Parent milestone: M5
- ADR-0012 (`docs/decisions/0012-authorial-intent-obligations-continuity-evals.md`)

## Must Not Do

- Do not let intent or obligation features introduce AI-managed session
  state; state transitions stay deterministic.
- Do not turn game-specific vocabulary into platform assumptions.
- Do not block M6 on AW-270 or AW-271.

## Architecture References

- `docs/decisions/0012-authorial-intent-obligations-continuity-evals.md`
- `docs/architecture/03-arc-execution.md`
- `docs/architecture/11-telemetry.md`

## Playtest Relevance

AW-272 strengthens the measurement quality of the M6 qualifying sessions;
the other two tasks harden the platform for the sessions after proof.
```

- [ ] **Step 2: Edit milestone file**

In `docs/roadmap/milestones/M5-hardening-proof-prerequisites.md`, after the M5-G bullet add:

```markdown
- [M5-H: Narrative Fidelity Layer](../epics/M5-H-narrative-fidelity-layer.md)
```

And append to the Exit Gate list:

```markdown
- The continuity and coherence eval suite (AW-272) runs against a synthetic session batch and reports knowledge-leak rate and contradiction count
```

- [ ] **Step 3: Verify and commit**

Run: `grep -c "M5-H" docs/roadmap/milestones/M5-hardening-proof-prerequisites.md`
Expected: `1`

```bash
git add docs/roadmap/epics/M5-H-narrative-fidelity-layer.md docs/roadmap/milestones/M5-hardening-proof-prerequisites.md
git commit -m "docs(roadmap): add M5-H narrative fidelity epic and M5 exit-gate line

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 3: index.json manifest entries

**Goal:** Register M5-H and the three tasks in `docs/roadmap/index.json` so the manifest matches the markdown source of truth.

**Files:**
- Modify: `docs/roadmap/index.json`

**Acceptance Criteria:**
- [ ] M5 milestone entry's `epics` array includes `"M5-H"`
- [ ] New epic entry for M5-H with `tasks: ["AW-272", "AW-270", "AW-271"]` and `"github": null`
- [ ] Three new task entries with correct `path`, `epic: "M5-H"`, `epic_id: "M5-H"`, `"github": null`; AW-270/271/272 sizes all `"M"`; `depends_on: []`
- [ ] File parses as JSON

**Verify:** `python -c "import json; d=json.load(open('docs/roadmap/index.json',encoding='utf-8-sig')); assert 'M5-H' in [e['id'] for e in d['epics']]; assert {'AW-270','AW-271','AW-272'} <= {t['id'] for t in d['tasks']}; print('OK')"` → `OK`

**Steps:**

- [ ] **Step 1: Apply edits with a Python script** (safer than hand-editing a large JSON file; preserve 1-space indent style used by the file)

```python
import json
p = 'docs/roadmap/index.json'
with open(p, encoding='utf-8-sig') as f:
    d = json.load(f)

m5 = next(m for m in d['milestones'] if m['id'] == 'M5')
if 'M5-H' not in m5['epics']:
    m5['epics'].append('M5-H')

d['epics'].append({
    'id': 'M5-H', 'milestone': 'M5', 'title': 'Narrative Fidelity Layer',
    'status': 'planned',
    'path': 'docs/roadmap/epics/M5-H-narrative-fidelity-layer.md',
    'tasks': ['AW-272', 'AW-270', 'AW-271'], 'github': None})

for tid, title, path in [
    ('AW-270', 'Authorial Intent Block and Intent Fidelity Telemetry',
     'docs/roadmap/tasks/AW-270-authorial-intent-block-and-fidelity-telemetry.md'),
    ('AW-271', 'Narrative Obligations Model and Reveal-Readiness Condition',
     'docs/roadmap/tasks/AW-271-narrative-obligations-model.md'),
    ('AW-272', 'Continuity and Coherence Eval Suite',
     'docs/roadmap/tasks/AW-272-continuity-coherence-eval-suite.md')]:
    d['tasks'].append({
        'id': tid, 'title': title, 'milestone': 'M5', 'epic': 'M5-H',
        'size': 'M', 'depends_on': [], 'path': path, 'github': None,
        'epic_id': 'M5-H'})

with open(p, 'w', encoding='utf-8') as f:
    json.dump(d, f, indent=1, ensure_ascii=False)
    f.write('\n')
print('index.json updated')
```

Match the existing key order of neighboring entries if it differs from the above; compare against the AW-267 entry before writing.

- [ ] **Step 2: Verify and commit**

Run the Verify command above. Expected: `OK`

```bash
git add docs/roadmap/index.json
git commit -m "docs(roadmap): register M5-H and AW-270/271/272 in roadmap manifest

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 4: Spec files 0064, 0065, 0066

**Goal:** Author the three per-feature specs using `docs/specs/0000-template.md` (Status, Author, References, Overview, In Scope, Out of Scope, Acceptance Criteria, Test Plan, Risks and Unknowns, Open Questions).

**Files:**
- Create: `docs/specs/0064-aw-270-authorial-intent-block.md`
- Create: `docs/specs/0065-aw-271-narrative-obligations-model.md`
- Create: `docs/specs/0066-aw-272-continuity-coherence-evals.md`

**Acceptance Criteria:**
- [ ] Each spec has Status "Approved" (founder approved scope via ADR-0012), Author "Founder + Claude (ADR-0012 rollout)", Date 2026-07-11
- [ ] Each references ADR-0012, the matching AW task file, and the architecture sections from Task 5
- [ ] Spec 0064 defines the exact `authorial_intent` JSON contract; spec 0065 defines the exact `obligations` DDL; spec 0066 defines the eval metrics and report format
- [ ] No em dashes

**Verify:** `ls docs/specs/006[456]*.md | wc -l` → 3; `grep -l "0012-authorial-intent" docs/specs/006[456]*.md | wc -l` → 3

**Steps:**

- [ ] **Step 1: Write spec 0064** with this content core (wrap in the 0000-template section skeleton):

Overview: Defines the optional `authorial_intent` ArcDefinition block and the intent fidelity telemetry contract for AW-270.

In Scope: schema block, validation rules, context-assembly injection, telemetry payloads. The JSON contract:

```json
"authorial_intent": {
  "theme": "string, required if block present",
  "tone": "string, required if block present",
  "emotional_targets": [
    {
      "beat_id": "string, must exist in beat_graph",
      "target_tension": "float in [0.0, 1.0]",
      "note": "string, optional"
    }
  ]
}
```

Validation rules: block optional; if present, `theme` and `tone` required, `emotional_targets` optional; every `beat_id` must exist in the arc's beat graph; `target_tension` in [0.0, 1.0]; unknown keys rejected. Telemetry: `tension_update` payload gains optional `target_score`; beat exit emits `intent_fidelity_summary` with `{beat_id, target_score, mean_score, mean_abs_deviation}` only for beats with a declared target.

Out of Scope: any AI-side interpretation of intent that mutates state; intent versioning or editing UI; runtime intent-drift interventions (open question in ADR-0012).

Acceptance Criteria: mirror the AW-270 task file checklist. Test Plan: unit tests for validation matrix (absent block, valid block, bad beat_id, out-of-range tension, unknown key), context-assembly snapshot test, telemetry payload tests, one headless harness integration run. Risks: prompt-layer growth vs caching (mitigate: block is a static cacheable layer); target curves may be badly authored (mitigate: telemetry reveals, does not enforce). Open Questions: none blocking; per-beat tone overrides deferred.

- [ ] **Step 2: Write spec 0065** with this content core:

Overview: Defines the `obligations` table, lifecycle, reveal-readiness condition, and telemetry for AW-271.

In Scope, DDL:

```sql
CREATE TABLE obligations (
    obligation_id  UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    session_id     UUID NOT NULL REFERENCES sessions(session_id),
    source_type    TEXT NOT NULL,   -- "authored" | "pacing_misdirection" | "generative"
    source_ref     JSONB NOT NULL DEFAULT '{}',  -- fact_id, event_id, or arc element id
    description    TEXT NOT NULL,
    mandatory      BOOLEAN NOT NULL DEFAULT false,
    status         TEXT NOT NULL DEFAULT 'open', -- "open" | "resolved" | "expired"
    created_beat   TEXT NOT NULL,
    resolved_beat  TEXT,
    created_at     TIMESTAMPTZ NOT NULL DEFAULT now(),
    resolved_at    TIMESTAMPTZ
);
CREATE INDEX ON obligations (session_id, status);
```

Lifecycle: only deterministic engine paths mutate obligation state (arc-authored setups registered at session start from the arc definition; pacing misdirection injection auto-creates one record; resolution happens on deterministic triggers such as fact delivery or beat transition, per arc configuration). Session context key `all_mandatory_obligations_resolved` computed on every context refresh; arcs reference it in `exit_conditions` exactly like any other condition key (Section 3.2 contract; the engine never interprets the name). Telemetry: `obligation_created` payload `{obligation_id, source_type, mandatory, beat_id}`; `obligation_resolved` payload `{obligation_id, resolution_beat, open_duration_seconds}`.

Out of Scope: AI-graded obligation resolution; obligation inference; UI surfaces.

Acceptance Criteria: mirror AW-271 checklist plus migration applies to empty and populated schemas. Test Plan: lifecycle unit tests, misdirection-injection integration test, exit-condition gating test in the headless harness, migration up/down test. Risks: obligation spam from repeated misdirections (mitigate: pacing engine already rate-limits interventions); schema churn (mitigate: JSONB `source_ref` keeps references flexible). Open Questions: expiration semantics for non-mandatory obligations at session end (default: mark `expired` on session completion).

- [ ] **Step 3: Write spec 0066** with this content core:

Overview: Defines the continuity and coherence eval suite for AW-272.

In Scope: runner `evals/runners/test_continuity_evals.py`; deterministic checks only in v1. Checks: (1) knowledge-leak: for every generated character output in a session log, compare referenced fact payloads against the character's knowledge state at generation time; any reference to a fact outside the state is a leak; (2) contradiction: a character output referencing a fact the character holds with a superseding revocation (per the revoke operation in `docs/architecture/04-knowledge-graph.md` Section 4.3) counts as a contradiction. Report JSON: `{"batch_id", "sessions": [{"session_id", "knowledge_leaks": int, "contradictions": int, "generations_checked": int}], "aggregate": {"knowledge_leak_rate": float, "contradiction_count": int}}`. Thresholds from a config file (`evals/continuity_thresholds.json`), defaults `knowledge_leak_rate <= 0.0` and `contradiction_count == 0` fail-fast values, adjustable without code changes.

Out of Scope: model-graded rubric evals (cost policy; revisit at Tier 2); runtime enforcement (that is the ADR-0012 watchpoint open question); intent-fidelity evals (depends on AW-270, post-M6).

Acceptance Criteria: mirror AW-272 checklist. Test Plan: seeded true-positive session fixture, clean-session false-positive guard, batch aggregation test over 10 synthetic sessions. Risks: fact-payload string matching misses paraphrased leaks (accepted v1 limitation, documented in the report header; classifier upgrade is the ADR-0012 watchpoint). Open Questions: whether eval batches run in CI on every PR or nightly (default: nightly plus pre-playtest manual run).

- [ ] **Step 4: Verify and commit**

Run: `grep -l "0012-authorial-intent" docs/specs/0064*.md docs/specs/0065*.md docs/specs/0066*.md | wc -l`
Expected: `3`

```bash
git add docs/specs/0064-aw-270-authorial-intent-block.md docs/specs/0065-aw-271-narrative-obligations-model.md docs/specs/0066-aw-272-continuity-coherence-evals.md
git commit -m "docs(specs): add specs 0064-0066 for ADR-0012 narrative fidelity features

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 5: Architecture section updates

**Goal:** Record the ADR-0012 architectural contracts in the three architecture files.

**Files:**
- Modify: `docs/architecture/03-arc-execution.md` (append Section 3.8 at end of file)
- Modify: `docs/architecture/11-telemetry.md` (append Section 11.8 at end of file)
- Modify: `docs/architecture/supplemental-schemas.md` (append `obligations` section at end of file)

**Acceptance Criteria:**
- [ ] Section 3.8 documents both the authorial intent block and the obligations contract with a pointer to specs 0064/0065
- [ ] Section 11.8 documents `target_score`, `intent_fidelity_summary`, `obligation_created`, `obligation_resolved`
- [ ] supplemental-schemas gains the `obligations` DDL matching spec 0065 exactly
- [ ] No em dashes in appended content

**Verify:** `grep -c "3.8\|11.8\|obligations" docs/architecture/03-arc-execution.md docs/architecture/11-telemetry.md docs/architecture/supplemental-schemas.md` → nonzero for each file

**Steps:**

- [ ] **Step 1: Append Section 3.8 to 03-arc-execution.md**

```markdown
## 3.8 Authorial Intent and Narrative Obligations (ADR-0012, post-M6)

Adopted in `docs/decisions/0012-authorial-intent-obligations-continuity-evals.md`.
Implementation is sequenced post-M6 under epic M5-H; this section records the
architectural contract so schema and engine work land against a stable design.

**Authorial intent block.** Arc definitions may declare an optional
`authorial_intent` block: `theme`, `tone`, and `emotional_targets` (a list of
`{beat_id, target_tension, note}` entries validated against the arc's beat
graph). When present, the block is injected into generation context assembly
as a static, cacheable context layer, and the pacing engine compares the live
`dramatic_tension_score` against the declared per-beat target. Intent is
generation context and telemetry comparison only: it never overrides
deterministic state transitions and it is never interpreted by the engine
beyond validation. Contract details: `docs/specs/0064-aw-270-authorial-intent-block.md`.

**Narrative obligations.** The engine tracks narrative obligations (authored
setups registered from the arc definition at session start, and any
pacing-engine misdirection injection, which auto-creates a record) as durable
rows in the `obligations` table (see `docs/architecture/supplemental-schemas.md`).
Only deterministic engine paths mutate obligation state; AI output never
creates or resolves an obligation directly. The session context exposes
`all_mandatory_obligations_resolved`, which arcs may reference in
`exit_conditions` exactly like any other condition key under the generic
evaluation contract in Section 3.2. The engine never interprets the key name.
Contract details: `docs/specs/0065-aw-271-narrative-obligations-model.md`.
```

- [ ] **Step 2: Append Section 11.8 to 11-telemetry.md**

```markdown
## 11.8 Narrative Fidelity Signals (ADR-0012)

Adopted in `docs/decisions/0012-authorial-intent-obligations-continuity-evals.md`.
The runtime signals below land with AW-270 and AW-271 (post-M6). The offline
continuity eval suite (AW-272, pre-M6) is the first consumer of the Tier 2
"behavior consistency score" signal listed in Section 11.6.

| Event | Payload | Emitted when |
| --- | --- | --- |
| `tension_update` (extended) | existing payload plus optional `target_score` | Arc declares an emotional target for the current beat |
| `intent_fidelity_summary` | `{beat_id, target_score, mean_score, mean_abs_deviation}` | Beat exit, for beats with a declared target |
| `obligation_created` | `{obligation_id, source_type, mandatory, beat_id}` | Authored setup registration or pacing misdirection injection |
| `obligation_resolved` | `{obligation_id, resolution_beat, open_duration_seconds}` | Deterministic resolution trigger fires |

Realized-versus-intended tension curves and obligation lifecycle data are
additional Tier 2 training signals collected at near-zero marginal cost.
```

- [ ] **Step 3: Append `obligations` section to supplemental-schemas.md** (heading style: match existing `## \`table\`` headings; use the exact DDL block from spec 0065 Step 2 above, introduced by one sentence: "Adopted by ADR-0012, implemented post-M6 by AW-271 with a reviewed migration. Tracks narrative obligations as durable session state.")

- [ ] **Step 4: Verify and commit**

Run: `grep -l $'—' docs/architecture/03-arc-execution.md docs/architecture/11-telemetry.md || echo CLEAN` (supplemental-schemas.md has one pre-existing em dash in a heading; only check the appended section visually)
Expected: `CLEAN` for the first two files

```bash
git add docs/architecture/03-arc-execution.md docs/architecture/11-telemetry.md docs/architecture/supplemental-schemas.md
git commit -m "docs(architecture): record ADR-0012 intent, obligations, and telemetry contracts

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 6: PRD updates

**Goal:** Reflect the adopted scope in the PRD requirements and scope files.

**Files:**
- Modify: `docs/prd/02-requirements.md` (append one paragraph at the end of the "What the Platform Must Do" section, after the "Session telemetry" paragraph)
- Modify: `docs/prd/03-scope.md` (append one bullet at the end of the "Future Directions (Post-MVP)" section)

**Acceptance Criteria:**
- [ ] 02-requirements gains the "Narrative fidelity" paragraph verbatim below
- [ ] 03-scope gains the Future Directions bullet verbatim below
- [ ] No em dashes in appended content

**Verify:** `grep -c "Narrative fidelity" docs/prd/02-requirements.md docs/prd/03-scope.md` → 1 in requirements; `grep -c "ADR-0012" docs/prd/03-scope.md` → at least 1

**Steps:**

- [ ] **Step 1: Append to 02-requirements.md** after the "Session telemetry" paragraph:

```markdown
**Narrative fidelity (ADR-0012):** Arc definitions may declare optional authored intent metadata: theme, tone, and per-beat emotional targets. When declared, the engine injects the block into generation context and logs realized-versus-intended pacing per beat. The engine tracks narrative obligations (authored setups and any pacing-injected misdirection) as durable session state and exposes a generic all-mandatory-obligations-resolved condition that arcs may use as a beat exit condition. Continuity is verified offline by an eval suite measuring knowledge-leak rate and character contradictions on synthetic sessions. The eval suite is pre-M6 hardening scope; intent and obligations implementation is sequenced post-M6 under epic M5-H.
```

- [ ] **Step 2: Append to 03-scope.md** at the end of "Future Directions (Post-MVP)":

```markdown
- **Narrative Fidelity Layer (ADR-0012):** Authorial intent as a structured ArcDefinition block with per-beat emotional targets, and a narrative obligations model with a reveal-readiness condition, are adopted platform scope sequenced post-M6 under epic M5-H. The companion continuity and coherence eval suite (AW-272) is pre-M6 hardening scope inside M5. See [ADR-0012](../decisions/0012-authorial-intent-obligations-continuity-evals.md).
```

- [ ] **Step 3: Verify and commit**

Run: `grep -c "ADR-0012" docs/prd/02-requirements.md docs/prd/03-scope.md`
Expected: `1` and `1` (or more)

```bash
git add docs/prd/02-requirements.md docs/prd/03-scope.md
git commit -m "docs(prd): add ADR-0012 narrative fidelity requirements and scope placement

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 7: GitHub issues + manifest backfill

**Goal:** Create the epic issue and three task issues per tracker conventions, then backfill live issue numbers into `docs/roadmap/index.json`.

**Files:**
- Modify: `docs/roadmap/index.json` (github objects for M5-H, AW-270, AW-271, AW-272)

**Acceptance Criteria:**
- [ ] Epic issue titled `M5-H: Narrative Fidelity Layer` with labels `Epic`, `M5`, milestone `M5: Hardening + Proof Prerequisites`, body from the epic file
- [ ] Three task issues titled `AW-270: ...`, `AW-271: ...`, `AW-272: ...` with labels `task`, `size:M`, `M5`, same milestone, bodies from the task files
- [ ] index.json `github` objects carry `issue_number` and `url` for all four
- [ ] index.json still parses

**Verify:** `gh issue list --repo nickejanssen/arcwright --label M5 --search "AW-27" --state open` shows the three tasks; the Task 3 Verify command still prints `OK`

**Steps:**

- [ ] **Step 1: Create the four issues** (capture each URL from stdout)

```bash
gh issue create --repo nickejanssen/arcwright \
  --title "M5-H: Narrative Fidelity Layer" \
  --body-file docs/roadmap/epics/M5-H-narrative-fidelity-layer.md \
  --label "Epic" --label "M5" \
  --milestone "M5: Hardening + Proof Prerequisites"

gh issue create --repo nickejanssen/arcwright \
  --title "AW-270: Authorial Intent Block and Intent Fidelity Telemetry" \
  --body-file docs/roadmap/tasks/AW-270-authorial-intent-block-and-fidelity-telemetry.md \
  --label "task" --label "size:M" --label "M5" \
  --milestone "M5: Hardening + Proof Prerequisites"

gh issue create --repo nickejanssen/arcwright \
  --title "AW-271: Narrative Obligations Model and Reveal-Readiness Condition" \
  --body-file docs/roadmap/tasks/AW-271-narrative-obligations-model.md \
  --label "task" --label "size:M" --label "M5" \
  --milestone "M5: Hardening + Proof Prerequisites"

gh issue create --repo nickejanssen/arcwright \
  --title "AW-272: Continuity and Coherence Eval Suite" \
  --body-file docs/roadmap/tasks/AW-272-continuity-coherence-eval-suite.md \
  --label "task" --label "size:M" --label "M5" \
  --milestone "M5: Hardening + Proof Prerequisites"
```

If a label or milestone is rejected, check exact names with `gh label list --repo nickejanssen/arcwright` and `gh api repos/nickejanssen/arcwright/milestones --jq '.[].title'`; do not invent new labels.

- [ ] **Step 2: Backfill issue numbers into index.json** (substitute the four numbers captured in Step 1)

```python
import json
p = 'docs/roadmap/index.json'
with open(p, encoding='utf-8') as f:
    d = json.load(f)
numbers = {'M5-H': EPIC_NUM, 'AW-270': N270, 'AW-271': N271, 'AW-272': N272}
base = 'https://github.com/nickejanssen/arcwright/issues/'
for e in d['epics']:
    if e['id'] in numbers:
        e['github'] = {'issue_number': numbers[e['id']], 'url': base + str(numbers[e['id']])}
for t in d['tasks']:
    if t['id'] in numbers:
        t['github'] = {'issue_number': numbers[t['id']], 'url': base + str(numbers[t['id']])}
with open(p, 'w', encoding='utf-8') as f:
    json.dump(d, f, indent=1, ensure_ascii=False)
    f.write('\n')
print('backfilled')
```

- [ ] **Step 3: Verify and commit**

Run: `python -c "import json; d=json.load(open('docs/roadmap/index.json',encoding='utf-8-sig')); e=[x for x in d['epics'] if x['id']=='M5-H'][0]; assert e['github']['issue_number']; print('OK')"`
Expected: `OK`

```bash
git add docs/roadmap/index.json
git commit -m "chore(tracker): create M5-H and AW-270/271/272 issues, backfill manifest numbers

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 8: Consistency sweep + PR

**Goal:** Verify every cross-reference resolves, nothing unintended is staged, and open the PR.

**Files:**
- None created; verification and PR only

**Acceptance Criteria:**
- [ ] Every file path referenced by the new/edited docs exists (specs reference tasks, tasks reference specs, epic references tasks, ADR references hold)
- [ ] `git status` shows no staged agent-local files and `docs/roadmap/operations/cloud-deploy-runbook.md` remains unstaged/untouched by this branch
- [ ] No em dash in any file added or edited on this branch
- [ ] PR opened against `main`

**Verify:** the check script below prints all `OK`; `gh pr view --json url` returns a URL

**Steps:**

- [ ] **Step 1: Cross-reference check**

```bash
python - <<'EOF'
import os, re, sys
files = [
 'docs/roadmap/tasks/AW-270-authorial-intent-block-and-fidelity-telemetry.md',
 'docs/roadmap/tasks/AW-271-narrative-obligations-model.md',
 'docs/roadmap/tasks/AW-272-continuity-coherence-eval-suite.md',
 'docs/roadmap/epics/M5-H-narrative-fidelity-layer.md',
 'docs/specs/0064-aw-270-authorial-intent-block.md',
 'docs/specs/0065-aw-271-narrative-obligations-model.md',
 'docs/specs/0066-aw-272-continuity-coherence-evals.md']
ok = True
for f in files:
    if not os.path.exists(f):
        print('MISSING', f); ok = False; continue
    text = open(f, encoding='utf-8').read()
    if '—' in text:
        print('EM DASH in', f); ok = False
    for ref in re.findall(r'`(docs/[^`]+\.(?:md|csv|json))`', text):
        if not os.path.exists(ref):
            print('BROKEN REF', ref, 'in', f); ok = False
print('OK' if ok else 'FAIL'); sys.exit(0 if ok else 1)
EOF
```

Expected: `OK`

- [ ] **Step 2: Confirm clean status**

Run: `git status --short`
Expected: only ` M docs/roadmap/operations/cloud-deploy-runbook.md` (pre-existing, leave alone) and nothing else uncommitted; no `.claude/` entries.

- [ ] **Step 3: Push and open PR**

```bash
git push -u origin docs/adr-0012-rollout
gh pr create --repo nickejanssen/arcwright --base main \
  --title "docs: ADR-0012 narrative fidelity rollout (M5-H, AW-270/271/272)" \
  --body "Rolls ADR-0012 through all canonical surfaces: roadmap task files and epic M5-H, index.json manifest, specs 0064-0066, architecture sections 3.8/11.8/supplemental-schemas, PRD requirements and scope, and the four tracker issues with manifest backfill.

Approval evidence: docs/decisions/0012-authorial-intent-obligations-continuity-evals.md (Accepted), D-068 in docs/product/decisions-log.csv.

🤖 Generated with [Claude Code](https://claude.com/claude-code)"
```

- [ ] **Step 4: Hand off to review**

Run the `review-pr` skill against the open PR before merge.

---

## Self-Review Notes

- Spec coverage: ADR-0012 item 1 → Tasks 1/4/5/6 (AW-270, spec 0064, section 3.8, 11.8, PRD); item 2 → Tasks 1/4/5/6 (AW-271, spec 0065, obligations DDL); item 4 → Tasks 1/4 (AW-272, spec 0066) plus M5 exit-gate line in Task 2. Deferred items 3/5 already live in the open-questions log (done pre-plan); no task needed.
- Type consistency: `authorial_intent` field names (`theme`, `tone`, `emotional_targets`, `beat_id`, `target_tension`, `note`) and telemetry names (`target_score`, `intent_fidelity_summary`, `obligation_created`, `obligation_resolved`) are identical across Task 1, 4, 5, and 6 content. Obligations DDL in Task 4 and Task 5 is the same block.
- Placeholder scan: all file content is fully drafted; the only substitutions at execution time are the four captured issue numbers in Task 7 Step 2, which cannot be known in advance.
