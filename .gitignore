__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/
.venv/
.venv-broken-uv/
node_modules/

evals/reports/*
!evals/reports/.gitkeep

# Agent-local state (session files, per-user settings — not project config)
# .codex/ is intentionally NOT listed here because .codex/environments/ is tracked project config
.claude/
.cursor/
